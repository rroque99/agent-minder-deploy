# Local environment — the only file you edit; contains registry credentials
# and the Elasticsearch password captured in Lab 4.
.env

# Generated: values/*.yaml.tpl rendered from .env at install time
.rendered/
