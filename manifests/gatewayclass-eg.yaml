# Lab 3 - GatewayClass for AgentMinder to reference.
# Referenced from ssp-override.yaml as ssp.ingress.gatewayApi.gatewayClassName
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: eg
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
